^Palettes:
	OverlayPlayerColorPalette@RAUNIT:
		BasePalette: player
		BaseName: playerraunit
		RemapIndex: 80, 81, 82, 83, 84, 85, 86, 87, 88, 89, 90, 91, 92, 93, 94, 95
		PlayerColors:
			Nod: E6E6FF
	OverlayPlayerColorPalette@TDUNIT:
		BasePalette: temptd
		BaseName: playertdunit
		RemapIndex: 176, 178, 180, 182, 184, 186, 189, 191, 177, 179, 181, 183, 185, 187, 188, 190
		PlayerColors:
			Nod: E6E6FF

^TDPalette:
	RenderSprites:
		PlayerPalette: playertdunit

^Infantry:
	RenderSprites:
		PlayerPalette: playertdunit
	WithDeathAnimation:
		DeathSequencePalette: playertdunit

SHAD:
	RenderSprites:
		PlayerPalette: playertd
	WithDeathAnimation:
		DeathSequencePalette: playertd

TPLR:
	RenderSprites:
		PlayerPalette: playertd
	WithDeathAnimation:
		DeathSequencePalette: playertd

RMBC:
	RenderSprites:
		PlayerPalette: playertd
	WithDeathAnimation:
		DeathSequencePalette: playertd

ENLI:
	RenderSprites:
		PlayerPalette: playertd
	WithDeathAnimation:
		DeathSequencePalette: playertd

^BuildingTD:
	RenderSprites:
		PlayerPalette: playertd

^DefenseTD:
	RenderSprites:
		PlayerPalette: playertd

SAPC:
	RenderSprites:
		PlayerPalette: playerraunit

HARV.TD:
	RenderSprites:
		PlayerPalette: playertd

AMCV:
	RenderSprites:
		PlayerPalette: playertd

LST:
	RenderSprites:
		PlayerPalette: playerraunit

SB:
	RenderSprites:
		PlayerPalette: playerraunit

SS2:
	RenderSprites:
		PlayerPalette: playerraunit

ISUB:
	RenderSprites:
		PlayerPalette: playerraunit
